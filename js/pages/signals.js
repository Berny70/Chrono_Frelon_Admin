// ── pages/signals.js — Gestion des signalements ──────────────
// Dépend de : config.js, db.js, auth.js, ui.js, dashboard.js
// (vide pour l'instant — la logique est dans dashboard.js)
// Ce fichier est réservé pour des fonctions spécifiques aux
// signalements si elles deviennent trop nombreuses.

const Signals = (() => {
  function init() {
    // Rien à initialiser ici pour l'instant
    // La suppression et le filtrage sont gérés dans dashboard.js
  }
  return { init };
})();
